The standing corner brand label "Gümrükte Güncel" — usually rendered for you by `PostFrame`.

```jsx
<BrandTag handle="@gumrukteguncel" />
```

- The 🛃 customs emoji is **off by default** (it renders full-color and clashes with the navy/white/yellow palette); enable it with `customs` only if a context truly calls for it.
- Reach for it directly only when building a layout outside `PostFrame`.
