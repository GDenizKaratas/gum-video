Standing topic row separated by yellow middots.

```jsx
<TopicChips items={['İthalat', 'İhracat', 'Vergi', 'Lojistik', 'Mevzuat']} />
```

- Faint uppercase by default; pass `tone="accent"` to brighten. Used on welcome / identity posts.
