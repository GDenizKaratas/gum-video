Solid yellow band with navy text — the single hardest-hitting line of a warning post.

```jsx
<WarningStrip icon="alert">Gümrük vergisinin ¼'ü ceza + araç men</WarningStrip>
```

- Optional leading `icon` (any `LineIcon` name, or `null`). One per post.
