Small uppercase, wide-tracked label above a headline.

```jsx
<Eyebrow tone="accent" rule>2026 GÜNCEL</Eyebrow>
<Eyebrow>BİLGİ NOTU</Eyebrow>
```

- `tone="accent"` (yellow) for date/news stamps; `tone="faint"` (default) for neutral categories.
- `rule` adds a short leading bar.
